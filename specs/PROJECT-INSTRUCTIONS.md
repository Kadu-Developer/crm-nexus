# PROJECT-INSTRUCTIONS

Primary entry:
`@specs\.claude\commands\prospectflow-speckit-orchestrator.md`

Start Claude with:
`claude --settings "specs/.claude/settings.json"`

Then load the orchestrator file above.

Never skip a stage.
Only a PASS gate authorizes NEXT.
Keep both state files synchronized.
