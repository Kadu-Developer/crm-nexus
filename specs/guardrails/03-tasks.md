# GR-03 — Tasks
Every task requires ID, priority, dependencies, exact/probable files, verification and acceptance.
- P0/P1 cannot be silently deferred.
- Break vague tasks into independently verifiable units.
- Add characterization/failing tests before risky refactors when appropriate.
- Identify safe parallel work.
BLOCK any task that says only “implement the feature”.
